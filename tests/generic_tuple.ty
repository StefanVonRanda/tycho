fn gid(x: $T) -> T:
    return x

struct GBox($T):
    v: T

fn main():
    t := gid((3, 4))
    println(str(t.0 + t.1))            # 7
    b := GBox((10, 20))
    println(str(b.v.0 + b.v.1))        # 30
    a := gid([1, 2, 3])
    println(str(a[0] + len(a)))        # 4
