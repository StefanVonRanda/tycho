type Id = int

fn take(x: Id) -> int:
    return to_int(x)

fn main():
    a := Id(3)
    b := Id(4)
    xs := [a, b]                # [Id], not [int]
    s := 0
    for x in xs:
        s = s + take(x)         # x is an Id
    println(str(s))             # 7
    println(str(take(xs[1])))   # 4  (index yields an Id)

    ys := [Id(10), Id(20), Id(30)]   # literal of constructors
    println(str(take(ys[2])))        # 30
