
enum Col:
    Red
    Blue

struct K:
    a: int

fn main():
    e := [Red: 1, Blue: 2]                 # fieldless-enum keys
    println(str(e[Red] + e[Blue]))         # 3

    s := [K(1): 10, K(2): 20]              # struct keys (deep-hashed)
    println(str(s[K(1)] + s[K(2)]))        # 30

    t := [(1, 2): 100, (3, 4): 200]        # tuple keys
    println(str(t[(1, 2)] + t[(3, 4)]))    # 300
