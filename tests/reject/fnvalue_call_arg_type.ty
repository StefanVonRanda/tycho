# src/tychoc.c:6733 -- the argument types of an indirect call on a fn value.
# expect: argument 2 must be int
fn add(a: int, b: int) -> int:
    return a + b

fn main():
    fs := [add]
    println(str(fs[0](1, "x")))
