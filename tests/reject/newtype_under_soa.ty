# 03-types.md:334-338: `soa` is in neither the permitted nor the forbidden list;
# it is excluded by the permitted list being closed ("MUST be one of"), and
# src/tychoc.c:4407-4409 refuses it (a soa is not is_array/is_map/IS_STRUCT).
# expect: a newtype's underlying type must be int, float, string, bool, an array, a map, or a struct (got soa [P])
struct P:
    x: int

type A = soa[P]

fn main():
    print("unreachable")
