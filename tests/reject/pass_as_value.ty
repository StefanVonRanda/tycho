# expect: `pass` is a statement and produces no value -- a value if/match needs one in every branch, so write this as a plain statement
fn main():
    x := if true:
        pass
    else:
        1
    println(str(x))
