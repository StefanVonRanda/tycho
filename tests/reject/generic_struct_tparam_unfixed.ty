# src/tychoc.c:6839 -- $U appears in no field, so no field value can fix it and
# the instance would have an unknown type argument.
# expect: type parameter $U of Box is not fixed by any field value
struct Box($T, $U):
    v: $T
fn main():
    b := Box(1)
    println(str(b.v))
