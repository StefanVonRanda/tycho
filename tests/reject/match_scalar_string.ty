# match on a string is refused: no dispatch customer in the tree; use if/elif
# (design: D1).
# expect: match on string is refused
fn main():
    s := "hi"
    match s:
        _:
            print("x")
