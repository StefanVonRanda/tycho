# src/tychoc.c:6701 -- a generic struct's field count is fixed by its
# declaration; the type arguments are what the values infer, not the arity.
# expect: Box takes 1 field value(s), got 2
struct Box($T):
    v: $T
fn main():
    b := Box(1, 2)
    println(str(b.v))
