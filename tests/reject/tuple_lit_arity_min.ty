# src/tychoc.c:6113. The TYPE rule is tests/reject/tuple_arity_min.ty; this is
# the LITERAL, and a trailing comma is the only spelling that reaches it --
# `(1)` is a grouping, not a one-element tuple (src/tychoc.c:2837-2840).
# expect: a tuple needs at least two elements
fn main():
    t := (1,)
    println(str(t.0))
