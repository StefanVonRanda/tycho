# tychoc reaches this arm by rooting the walk at an EARLIER-declared struct that
# only REFERENCES the cycle: src/tychoc.c:12863 visits every struct before any
# Option, Result or tuple, so the first grey node re-entered here is the Option,
# not B. tychoc1 attributes the same cycle to the struct that closes it, so only
# the shared prefix can be pinned.
# expect: infinite type:
# src/tychoc.c:12805
struct A:
    b: Option(B)

struct B:
    x: Option(B)

fn main():
    println("unreachable")
