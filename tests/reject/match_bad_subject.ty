# src/tychoc.c:8799 -- a struct has no cases to dispatch on.
# expect: match expects an Option, Result, or enum value, or an int/char/bool, got S
struct S:
    a: int

fn main():
    s := S(1)
    match s:
        _: println("other")
