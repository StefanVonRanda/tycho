# expect: lambda parameter
# A lambda parameter's type is optional only where an expected fn type supplies
# it; bound to a bare variable there is nothing to infer from.
fn main():
    f := fn(x): x + 1
    println(str(f(2)))
