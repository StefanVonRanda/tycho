# expect: 'void' is a type only as a Result's ok payload, as in Result(void, string)
# A function-type parameter cannot take void: there is no value to pass.
fn f(g: fn(void) -> int) -> int:
    return 1
fn main():
    println(str(f(fn(x: int): 1)))
