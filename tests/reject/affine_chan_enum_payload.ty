# Phase 20: ... nor an enum payload (tychoc: resolve_program, src/tychoc.c:7287).
# BEFORE this phase tychoc0 accepted this AND the emitted C compiled and RAN.
enum E:
    A(Channel(int))
    B

fn main():
    print("x")
