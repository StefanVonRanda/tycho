# expect: pop does not apply to a vector[2]int — its length is fixed
# The length IS the type; the reference once emitted a call to an undeclared
# tycho_arr_C0_pop here and the C compiler refused it.
fn main():
    v: vector[2]int = [1, 2]
    println(str(pop(v)))
    println(str(v[0]))
