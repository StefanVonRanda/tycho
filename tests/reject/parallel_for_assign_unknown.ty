# src/tychoc.c:8181 -- the parallel-for scan's own unknown-variable check, which
# runs before the ordinary one. Both compilers word it identically.
# expect: assignment to unknown variable 'zzqq'
fn main():
    xs := [1, 2, 3]
    parallel for i in 0..<3:
        zzqq = xs[i]
    println("x")
