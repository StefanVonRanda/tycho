# expect: which does not satisfy

fn distinct(xs: [$T]) -> int where hashable(T):
    m : [T: int] = []
    for x in xs:
        m[x] = 1
    return len(m)

fn main():
    println(str(distinct([1.5, 2.5])))
