# UFCS builtin desugar must fail closed on a wrong receiver type: `n.push(1)`
# lowers to `push(n, 1)`, and push requires an array receiver -> rejected.
# expect: push's first argument must be an array or soa
fn main():
    n := 42
    n.push(1)
