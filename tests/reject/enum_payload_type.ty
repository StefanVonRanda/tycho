# src/tychoc.c:6994 -- a payload value must have the declared payload type;
# there is no widening from string.
# expect: payload type mismatch
enum Shape:
    Circle(float)
fn main():
    s := Circle("x")
    println(str(len(str(s))))
