# Phase 20: an affine Channel(T) may not be a dynamic-array element (spec 03-types.md 5.3.9).
# tychoc rejects at arrc_sized_b (src/tychoc.c:745-747); tychoc0 at parse_type_d's [T] site.
fn f(v: [Channel(int)]) -> int:
    return len(v)

fn main():
    print("x")
