fn dbl(xs: sink [int]) -> int:
    xs[0] = xs[0] + 1
    return xs[0]

fn main():
    b := [5]
    r := dbl(b)                          # b is consumed here ...
    print(str(b[0]) + " " + str(r))      # ... but used again -> error
