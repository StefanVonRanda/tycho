# src/tychoc.c:8471 -- a value if/match branch whose tail is a void call.
# expect: a value if/match branch must produce a value, not void
fn nop():
    println("n")

fn main():
    x := if true:
        nop()
    else:
        nop()
    println("hi")
