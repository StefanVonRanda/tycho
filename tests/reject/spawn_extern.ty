# src/tychoc.c:5977 -- an FFI function has no thread-safety contract, so it may
# not be the body of a spawned task.
# expect: cannot spawn an extern (FFI) function
extern fn getpid() -> int

fn main():
    t := spawn getpid()
    println(str(wait(t)))
