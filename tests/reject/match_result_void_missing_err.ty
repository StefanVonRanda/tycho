# expect: match on a Result must cover both Ok and Err
fn touch(x: int) -> Result(void, string):
    if x < 0:
        return Err("neg")
    return Ok()

fn main():
    match touch(1):
        Ok:
            println("ok")
