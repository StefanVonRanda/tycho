# expect: 'total' instantiated with T = P, which does not satisfy `defaultable(T)`

struct P:
    x: int

fn total(xs: [$T]) -> T where defaultable(T):
    return zero$(T)

fn main():
    println(str(total([P(1), P(2)]).x))
