# expect: declared type Option(string) but value is Some(int)
fn g(o: Option(string)):
    match o:
        Some(s):
            println(s)
        None:
            println("none")

fn main():
    g(Some(42))
