# Phase 20: `Task(T)` is NOT a written type name -- a task handle is born from `spawn`.
# tychoc: `unknown type 'Task'` (src/tychoc.c:2071). BEFORE this phase tychoc0 handed the
# written spelling back as its own INTERNAL handle type: accepted, compiled and RAN.
fn f(v: Task(int)) -> int:
    return 1

fn main():
    print("x")
