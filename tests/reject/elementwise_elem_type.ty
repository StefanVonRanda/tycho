# src/tychoc.c:7443. Both compilers word it identically.
# expect: element-wise `+` requires two arrays with the same element type (got [int], [float])
fn main():
    a := [1, 2]
    b := [1.0, 2.0]
    c := a + b
    println(str(len(c)))
