# Phase 20: ... nor a struct field (tychoc: resolve_program, src/tychoc.c:7018).
# BEFORE this phase tychoc0 accepted this AND the emitted C compiled and RAN.
struct Box:
    c: Channel(int)

fn main():
    print("x")
