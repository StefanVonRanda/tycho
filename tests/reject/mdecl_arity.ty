# src/tychoc.c:8666 -- three names, two elements: the third would bind nothing.
# expect: destructuring 3 target(s) from a 2-element tuple
fn f() -> (int, int):
    return (1, 2)

fn main():
    a, b, c := f()
    println(str(a) + str(b) + str(c))
