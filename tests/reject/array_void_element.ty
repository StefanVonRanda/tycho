# expect: 'void' is a type only as a Result's ok payload, as in Result(void, string)
# An array of void would have a length and no storage; the element type is
# refused.
fn f(xs: [void]) -> int:
    return len(xs)
fn main():
    println(str(f([])))
