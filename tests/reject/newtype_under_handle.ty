# 03-types.md:334-338: a newtype MUST NOT wrap a typed handle.
# expect: newtype's underlying type must be
handle H:
    free: hfree

extern fn hfree(h: H) -> int

type A = H

fn main():
    print("unreachable")
