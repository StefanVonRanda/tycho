# expect: 'void' is a type only as a Result's ok payload, as in Result(void, string)
# A channel of void would carry no value, so the type is refused.
fn f(c: Channel(void)) -> int:
    return 1
fn main():
    println(str(f(channel(int, 1))))
