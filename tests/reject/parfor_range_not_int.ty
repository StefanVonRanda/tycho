# src/tychoc.c:8095 -- the `0..<N` bounds of a parallel for are int; a string
# bound has no count.
# expect: parallel for needs an int range
fn main():
    parallel for i in 0..<"3":
        println(str(i))
