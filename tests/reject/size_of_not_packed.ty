# expect: size_of$(int): only a packed or an align(N) struct has a byte size
# A byte size is only given for a packed or an align(N) struct; every other type's layout is
# the C compiler's business and this language does not promise it.
fn main():
    println(str(size_of$(int)()))
