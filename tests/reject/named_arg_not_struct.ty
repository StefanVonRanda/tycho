# src/tychoc.c:6826 -- `name: value` arguments exist for struct construction
# only; a function call has no field names to match them against.
# expect: named arguments are only valid for struct construction
fn f(x: int) -> int:
    return x
fn main():
    println(str(f(x: 1)))
