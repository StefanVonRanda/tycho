# src/tychoc.c:6757 -- a newtype wrap is one value, not a constructor call:
# there is exactly one underlying field to fill.
# expect: Meters(x) takes one float
type Meters = float
fn main():
    m := Meters(1.0, 2.0)
    println(str(to_float(m)))
