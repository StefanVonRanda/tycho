# src/tychoc.c:6483 -- the no-suggestion arm: 'zzzzzz' is far enough from
# 'alpha' that suggest_field offers nothing.
# expect: struct S has no field 'zzzzzz'
struct S:
    alpha: int
fn main():
    s := S(1)
    println(str(s.zzzzzz))
