# expect: infinite type: A contains itself by value — use an array ([A]) or Option([A]) for indirection
struct A:
    b: B
    n: int

struct B:
    a: A
    n: int

fn main():
    println("unreachable")
