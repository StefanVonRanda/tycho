# src/tychoc.c:8870 -- an Option has exactly Some, None and `_`; anything else
# names a variant it does not have.
# expect: an Option's arms are Some(x), None, and _, not 'Nope'
fn main():
    x : Option(int) = Some(1)
    match x:
        Some(v): println(str(v))
        Nope: println("n")
