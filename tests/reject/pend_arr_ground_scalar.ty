# A bare `[]` declaration is PENDING: its first typed use grounds it, and only an
# array, map or soa can. src/tychoc.c:6174.
# expect: 'xs' was declared with [] but its first use makes it int
fn main():
    xs := []
    xs = 5
    println(str(xs))
