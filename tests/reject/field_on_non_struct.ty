# src/tychoc.c:6447 -- `.f` on a value that is not a struct at all, which is a
# different rule from a struct that merely lacks the field.
# expect: '.foo' on a non-struct value
fn main():
    x := 1
    println(str(x.foo))
