# src/tychoc.c:9644 -- a `[$N]T` size parameter is fixed by a generic fn's ARGUMENT.
# In a stored position nothing infers N, so it is refused rather than emitted as a
# phantom array type.
# expect: an enum payload cannot be a `[$N]T` size-parameterized array -- use a fixed `[N]T` or a dynamic `[T]`
enum E:
    V([$N]int)

fn main():
    println("unreachable")
