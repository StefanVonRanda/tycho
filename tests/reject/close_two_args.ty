# src/tychoc.c:6927 -- close takes exactly one channel or handle.
# tychoc1 ACCEPTED this under --typecheck until the arity check was ported
# into compiler/types/tcheck.ty; both now word it identically.
# expect: close takes one channel or handle
fn main():
    ch := channel(int, 1)
    close(ch, 1)
