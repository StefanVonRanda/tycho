# src/tychoc.c:7401 -- the mirror of inout_arg_not_place.ty: an '&' on a
# parameter that is not inout. Both compilers word it identically.
# expect: argument 1 of 'plain' is not inout; remove the '&'
fn plain(x: int):
    println(str(x))
fn main():
    y := 1
    plain(&y)
