# §14.4 (docs/spec/10-statements.md:52): "`for C:` runs its body while the
# `bool` condition `C` holds." Tycho has no `while`; `for COND:` is the while
# form, and its condition is subject to the same rule as `if`'s.
fn main():
    for 1:
        break
