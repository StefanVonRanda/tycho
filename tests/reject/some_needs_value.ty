# src/tychoc.c:6078 -- Some(x) wraps a VALUE; a call to a procedure is none.
# expect: Some(...) needs a concrete value
fn nop():
    println("x")

fn main():
    s := Some(nop())
    if s != None:
        println("y")
