# 15-program.md §27.1 / src/tychoc.c:7449-7450 (resolve_program): `main` MUST take no parameters.
# Accepted by tychoc0 before the front-door plan, and it COMPILED AND RAN.
fn main(x: int):
    print(str(x))
