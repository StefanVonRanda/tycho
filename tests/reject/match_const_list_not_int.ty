# src/tychoc.c:9292@pcname -- a const NAME inside a pattern list, as opposed to the bare
# `C:` arm shape at src/tychoc.c:9270@literals. tychoc1 reaches its own bare-arm wording, which
# carries this message as a prefix, so the shared part is what is asserted.
# expect: 'C' is not an int constant
const C = "x"
fn main():
    n := 1
    match n:
        C | 2: println("a")
        _: println("b")
