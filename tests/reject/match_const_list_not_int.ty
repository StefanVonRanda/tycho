# src/tychoc.c:8785 -- a const NAME inside a pattern list, as opposed to the bare
# `C:` arm shape at :8635. tychoc1 reaches its own :8635-style wording, which
# carries this message as a prefix, so the shared part is what is asserted.
# expect: 'C' is not an int constant
const C = "x"
fn main():
    n := 1
    match n:
        C | 2: println("a")
        _: println("b")
