# src/tychoc.c:9486 -- the newtype sibling of enum_payload_sizeparam.ty. It gets
# PAST the underlying-type rule at src/tychoc.c:4941 because `[$N]int` is an array;
# the channel arm of the same sweep does not, which is why that one has no fixture.
# expect: a newtype cannot wrap a `[$N]T` size-parameterized array
fn take(a: [$N]int) -> int:
    return a[0]

type Arr = [$N]int

fn main():
    println("unreachable")
