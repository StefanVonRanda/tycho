# src/tychoc.c:6463 -- `x[i]` on a scalar. tests/reject/tuple_index.ty is the
# tuple near miss, which gets its own message.
# expect: can only index an array, a string, bytes, or a map (as a place)
fn main():
    b := true
    println(str(b[0]))
