# `is` only asks an ENUM which variant it holds. A non-enum left operand is a
# compile error rather than a test that can never be true.
# expect: which variant it holds; int is none of those
fn main():
    x := 3
    if x is VInt:
        println("unreachable")
