# src/tychoc.c:6677 -- an indirect call on a fn VALUE read out of an array. The
# fn-typed LOCAL twin is fnvar_call_arity.ty, and it is a different code path
# with a different message.
# expect: this function value expects 2 argument(s), got 1
fn add(a: int, b: int) -> int:
    return a + b

fn main():
    fs := [add]
    println(str(fs[0](1)))
