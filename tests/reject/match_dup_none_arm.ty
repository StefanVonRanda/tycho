# src/tychoc.c:8535 -- None is nullary, so a second None arm is unreachable.
# expect: duplicate None arm
fn main():
    x : Option(int) = Some(1)
    match x:
        Some(v): println(str(v))
        None: println("n1")
        None: println("n2")
