# 05-generics.md:80-82 / src/tychoc.c:2291: same rule, generic enum.
# expect: its type arguments must be EITHER exactly its own parameters
enum Opt($T):
    N
    S($T)

fn f(x: $T) -> Opt([$T]):
    return N

fn main():
    print("unreachable")
