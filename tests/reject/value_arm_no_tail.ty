# expect: a value branch must end in a value expression
fn main():
    x := if true:
        acc := 0
        for i := 0; i < 3; i += 1:
            acc = acc + i
    else:
        9
    println(str(x))
