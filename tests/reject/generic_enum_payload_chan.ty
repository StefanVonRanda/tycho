# expect: an enum payload cannot be a channel

enum Holder($T):
    Empty
    Full($T)

fn main():
    ch := channel(int, 2)
    h := Full(ch)
    match h:
        Full(c): send(c, 5)
        Empty: println("empty")
