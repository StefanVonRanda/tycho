# src/tychoc.c:2967 -- the soa LITERAL spelling, a second site with its own
# message: the type is written `soa [T]` and the empty literal `soa []T`.
# expect: soa requires a struct element type, e.g. soa []Point
fn main():
    xs := soa []int
    println(str(len(xs)))
