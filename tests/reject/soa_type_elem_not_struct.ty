# src/tychoc.c:2410 -- the soa TYPE spelling. An SOA is a struct-of-arrays, so
# there is nothing to split a scalar element into.
# expect: soa requires a struct element type, e.g. soa [Point]
struct P:
    x: int

fn main():
    xs: soa [int] = soa []P
    println(str(len(xs)))
