# src/tychoc.c:6077 -- `wait(t)` yields the spawned function's value, so a task
# over a fn with no return type would yield nothing.
# expect: a spawned function must return a value (wait(t) yields it)
fn nop(i: int):
    println(str(i))

fn main():
    t := spawn nop(1)
    wait(t)
