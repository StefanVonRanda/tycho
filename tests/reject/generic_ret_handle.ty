# expect: a Tycho fn cannot return a handle

handle R:
    free: hc

extern fn ho(i: int) -> R
extern fn hc(h: R) -> int

fn ident(x: $T) -> $T:
    return x

fn main():
    f := ho(1)
    g := ident(f)
    print("unreachable")
