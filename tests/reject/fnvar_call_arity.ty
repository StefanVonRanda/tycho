# src/tychoc.c:6712 -- a call through a fn-typed LOCAL. It names the variable,
# where the fn-value twin fnvalue_call_arity.ty cannot.
# expect: 'f' expects 2 argument(s), got 1
fn add(a: int, b: int) -> int:
    return a + b

fn main():
    f := add
    println(str(f(1)))
