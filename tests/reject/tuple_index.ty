# A tuple is not indexable. Deliberate, and the sibling of the one-binder `for`
# rule in tests/reject/for_two_binders.ty: destructure in an assignment instead.
# expect: a tuple element is named
fn main():
    t := ("a", 1)
    println(t[0])
