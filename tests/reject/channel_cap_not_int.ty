# src/tychoc.c:6908 -- a channel's capacity is exactly int.
# tychoc1 ACCEPTED this until the rule was ported into compiler/types/tcheck.ty's
# ast.ChanE arm; both now word it identically.
# expect: channel(T, cap) needs an int capacity
fn main():
    ch := channel(int, "x")
    close(ch)
