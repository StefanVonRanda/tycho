# expect: cannot mix a fixed array and a growable array in element-wise `+` (got [2]int, [int]) -- copy one side into the other's kind first
fn main():
    a: [2]int = [1, 2]
    b := [1, 2]
    c := a + b
    println(str(len(c)))
