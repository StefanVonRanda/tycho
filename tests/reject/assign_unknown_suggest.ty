# src/tychoc.c:8565 -- destructuring assignment to a name that does not exist,
# close enough to an existing one for the suggester.
# expect: assignment to unknown variable 'counr' (use ':=' to declare); did you mean 'count'?
fn two() -> (int, int):
    return 1, 2
fn main():
    count := 0
    a := 0
    a, counr = two()
    println(a)
