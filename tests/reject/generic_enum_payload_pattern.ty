# src/tychoc.c:6780 -- the payload pattern is [$T]; an int unifies with no
# array, so there is nothing to bind $T to.
# expect: Wrap payload 1 does not fit a int argument
enum Box($T):
    Wrap([$T])
fn main():
    b := Wrap(1)
    println(str(len(str(b))))
