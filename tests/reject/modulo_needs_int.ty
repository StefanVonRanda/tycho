# src/tychoc.c:7575 -- modulo and the bitwise operators are integer-only.
# expect: modulo / bitwise operators require two matching integers (got float, float)
fn main():
    x := 1.5
    println(str(x % 2.0))
