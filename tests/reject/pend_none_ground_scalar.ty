# The `None` half of the pending pair above: a bare `None` declaration needs an
# Option to ground it. src/tychoc.c:6017.
# expect: 'x' was declared with None but its first use makes it int
fn main():
    x := None
    x = 5
    println(str(x))
