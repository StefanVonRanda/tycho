# expect: reserve does not apply to a [4]int — its size is fixed
# The length IS the type; the reference once emitted a call to an undeclared
# tycho_arr_C0_reserve here and the C compiler refused it.
fn main():
    v: [4]int = [1, 2, 3, 4]
    reserve(v, 8)
    println(str(v[0]))
