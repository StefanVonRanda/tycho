# src/tychoc.c:6845@payload -- the qualified twin of src/tychoc.c:6642@payload.
# expect: lib.Tick carries a payload — write lib.Tick(...)
package main
import "lib"

fn main():
    e := lib.Tick
    match e:
        lib.Tick(n): println(str(n))
        lib.Stop: println("s")
