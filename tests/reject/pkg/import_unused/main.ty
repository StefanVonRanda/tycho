# Go's rule: an import nothing in its file uses is an error. src/tychoc.c:5188.
# It needs a `package` header to import at all, so it lives here rather than flat.
# expect: `core:strings` imported and not used in this file
package main

import "core:strings"

fn main():
    println("no qualifier reaches the import")
