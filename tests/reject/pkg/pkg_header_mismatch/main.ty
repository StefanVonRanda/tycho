# ...and the header it carries must name the directory it is in.
# src/tychoc.c:14412, the sibling of pkg_header_missing.
# expect: declares `package elsewhere` but is in package `wrongname`
package main
import "wrongname"

fn main():
    println(str(wrongname.two()))
