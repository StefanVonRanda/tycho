# src/tychoc.c:6528 -- a package-qualified name taken as a VALUE, with no
# near-miss for the suggester (fnval_pkg_missing.ty covers the suggesting arm).
# Both compilers word it identically.
# expect: package 'strings' has no variant, const or function 'zzqqww'
package main
import "core:strings"

fn main():
    f := strings.zzqqww
    println("x")
