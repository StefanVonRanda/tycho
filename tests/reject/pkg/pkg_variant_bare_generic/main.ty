# src/tychoc.c:6422 -- a nullary variant of a generic enum fixes no $T, and the
# qualified spelling has its own message (the bare one at :6130 names the enum).
# expect: lib.Empty is a variant of a generic enum; supply the type explicitly, e.g. lib.Empty$(int)
package main
import "lib"

fn main():
    b := lib.Empty
    match b:
        lib.Full(v): println(str(v))
        lib.Empty: println("e")
