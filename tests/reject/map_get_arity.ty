# src/tychoc.c:6529 -- the m.get sugar takes a key and an optional default.
# expect: m.get(key) or m.get(key, default) takes one or two arguments (got 3)
fn main():
    m := []string: int
    m["a"] = 1
    v := m.get("a", 0, 9)
    println(str(v))
