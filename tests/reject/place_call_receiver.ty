# expect: can only index-assign an array or map variable or field
struct Inner:
    v: [int]

struct Box:
    inr: Inner

fn get(b: Box) -> Inner:
    return b.inr

fn main():
    b := Box(Inner([1]))
    b.get().v[0] = 9
