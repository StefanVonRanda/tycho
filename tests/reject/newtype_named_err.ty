# A newtype shares the type namespace with struct/enum/handle, so it gets the
# same builtin-constructor guard. Sibling: tests/reject/struct_named_ok.ty.
# expect: 'Err' is already defined
type Err = int

fn main():
    println("unreachable")
