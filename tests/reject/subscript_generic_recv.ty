# expect: a subscript receiver may not be generic ('Pool($T)'): declare the subscript on a concrete instance
struct Pool($T):
    items: [$T]

subscript at(p: Pool($T), i: int) -> inout $T:
    yield &p.items[i]

fn main():
    println("unreachable: the declaration above is refused")
