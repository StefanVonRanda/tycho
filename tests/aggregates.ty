struct P:
    x: int
    y: int

struct Named:
    name: string
    score: int

fn sumx(ps: [P]) -> int:
    t := 0
    for i in range(len(ps)):
        t = t + ps[i].x
    return t

fn addp(ps: mut [P], p: P):
    push(ps, p)

fn make_row(n: int) -> [int]:
    r := []int
    for i in range(n):
        push(r, i * i)
    return r

fn main():
    ps := [P(1, 2), P(3, 4)]
    push(ps, P(5, 6))
    println("len=" + str(len(ps)) + " sumx=" + str(sumx(ps)) + " p2x=" + str(ps[2].x))
    ps[0] = P(100, 200)
    println("set0x=" + str(ps[0].x))
    qs := ps
    push(qs, P(7, 8))
    println("ps_len=" + str(len(ps)) + " qs_len=" + str(len(qs)))
    addp(&ps, P(9, 9))
    println("after_inout len=" + str(len(ps)) + " last_x=" + str(ps[3].x))

    ns := [Named("ada", 10), Named("alan", 20)]
    ms := ns
    ns[0] = Named("Ada", 99)
    println("ns0=" + ns[0].name + " ms0=" + ms[0].name)

    grid := [][int]
    push(grid, make_row(3))
    push(grid, make_row(4))
    println("grid_len=" + str(len(grid)) + " row1_2=" + str(grid[1][2]) + " grid1len=" + str(len(grid[1])))

    a := [P(1, 1), P(2, 2)]
    b := [P(1, 1), P(2, 2)]
    if a == b:
        println("eq-ok")
