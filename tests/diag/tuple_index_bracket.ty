# `t[0]` is the C habit; a tuple element is `.0`. The old message listed array,
# string, bytes and map and never named the form that works. Agent probe,
# 2026-08-20.
fn p() -> (int, string):
    return (1, "a")

fn main():
    t := p()
    println(str(t[0]))
