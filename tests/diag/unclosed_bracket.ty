# An unclosed bracket used to swallow the rest of the file as one continuation
# line, killing batching and inventing an error on a valid line. Two mistakes,
# two errors, each naming the bracket's own line.
fn a(:
    return 1

fn b(:
    return 2

fn main():
    println("x")
