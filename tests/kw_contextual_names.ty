struct Q:
    where: int
    soa: bool
    range: int

fn sink(extern: int, package: int) -> int:
    return extern + package

fn main():
    q := Q(1, true, 2)
    where := q.where + q.range
    range := sink(where, 4)
    println(str(range))
    println(str(q.soa))
