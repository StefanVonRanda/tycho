struct Box($T):
    v: $T

struct W($T):
    inner: Box($T)
    n: int

fn main():
    w := W(Box(7), 3)
    println(str(w.inner.v + w.n))          # 10
    s := W(Box("hi"), 0)
    println(s.inner.v + "!")                     # hi!
