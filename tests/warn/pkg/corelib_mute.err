tests/warn/pkg/corelib_mute/main.ty:13: warning: `len` collides with the builtin of the same name -- every unqualified `len(...)` calls the BUILTIN, so this procedure is unreachable by that name (§3.7). `package main` has no package prefix to qualify with, so rename it.
    13 | fn len(s: string) -> int:
