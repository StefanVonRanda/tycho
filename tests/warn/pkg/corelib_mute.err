tests/warn/pkg/corelib_mute/main.ty:13: warning: `len` collides with the builtin of the same name -- every unqualified `len(...)` calls the BUILTIN, so this procedure is unreachable by that name (§3.7). Rename it, or call it qualified as `pkg.len(...)`.
    13 | fn len(s: string) -> int:
