package fnval

fn base(x: int) -> int:
    return x + 1

fn apply(f: fn(int) -> int, x: int) -> int:
    return f(x)

fn via_local(x: int) -> int:
    g := base                  # bare same-package fn as a value
    return g(x)                # indirect call through a local var

fn main():
    println("arg=" + str(apply(base, 41)))    # 42  (fn passed as a value within the package)
    println("local=" + str(via_local(9)))     # 10
    h := base
    println("bound=" + str(h(99)))            # 100
