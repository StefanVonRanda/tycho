package geom

struct Point:
    x: int
    y: int

enum Shape:
    Circle(int)
    Square(int)

fn area(s: Shape) -> int:
    match s:
        Circle(r):
            return r * r * 3
        Square(side):
            return side * side
