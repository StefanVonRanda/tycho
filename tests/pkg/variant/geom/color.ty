package geom

enum Color:
    Red
    Green
    Blue

fn name(c: Color) -> string:
    match c:
        Red:
            return "red"
        Green:
            return "green"
        Blue:
            return "blue"

fn default() -> Color:
    return Green                 # bare in-package variant value
