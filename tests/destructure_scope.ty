fn pair(s: string) -> (string, string):
    return (s, s)

fn build(x: string) -> string:
    a, b := pair(x)
    c, d := pair(b)
    e, f := pair(d)
    joined := a + c + e
    return joined

fn main():
    println(build("z"))
