
struct Box($T):
    xs: bounded[2]$T

fn main():
    xs: bounded[2]int = [1, 2]
    p := Box(xs)
    println("full at " + str(len(p.xs)))
    push(p.xs, 3)
    println("unreachable: pushed past the capacity")
