1. Add String interpolation, ${name} syntax — like JS template literals.
2. Add Raw strings, backtick-delimited.
3. Add Array programming, a * b element-wise on arrays.
4. Replace for cond with odin-style for i := 0; i < n; i += 1: / for i < n: / for:
