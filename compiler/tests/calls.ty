# Stage 2C: bare call statements — a void function invoked for its effect,
# with no assignment.
fn countdown(n: int):
    for n > 0:
        print(str(n) + " ")
        n = n - 1
    println("liftoff")

fn main():
    println("go:")
    countdown(3)
    println("done")
