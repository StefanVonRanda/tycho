# Compute-bound parallel reduction: sum (i*31+7) % 1000003 over 4e8 ints.
# hier: `parallel for` -- K = ncpu chunk tasks, identity partials, fold at join.
fn main():
    total := 0
    parallel for i in range(400000000):
        total += (i * 31 + 7) % 1000003
    print(str(total))
