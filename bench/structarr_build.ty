struct Item:
    name: string
    n: int

fn main():
    last := 0
    for i := 0; i < 1000000; i += 1:
        a := [Item("x" + str(i), i)]
        push(a, Item("y" + str(i), i))
        push(a, Item("z" + str(i), i))
        last = len(a)
    println(str(last))
