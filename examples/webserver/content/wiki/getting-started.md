# Getting Started

Build the compiler and run an example:

```
make
./tychoc examples/webserver/main.ty -o server
./server --serve
```

Then open `http://127.0.0.1:8080/`.

## Next steps

- Read the [Hello, Tycho](/blog/hello-tycho) post.
- Skim the language reference in `docs/`.
