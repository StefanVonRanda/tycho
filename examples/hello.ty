# The success-criterion demo: ask for a name, greet it.

fn greet(name: string) -> string:
    return "hello " + name

fn main():
    print("what is your name: ")
    name := input()
    println(greet(name))
